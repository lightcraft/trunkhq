.tabbable
  %ul#locations.nav.nav-tabs
    - @locations.each do |location|
      - ident = dom_id(location)
      %li{:class => "#{ 'active' if !@location.nil? && @location.eql?(location)}"}= link_to location.name, '#'+ident, {:anchor => ident, :'data-toggle' => 'tab'}

.tab-content
  - @locations.each do |location|
    = div_for(location, :class => 'tab-pane') do
      loading...

#sys_info.modal.hide
#edit
